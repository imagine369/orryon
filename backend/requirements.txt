# ── FastAPI backend ────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pyjwt>=2.8.0
python-multipart>=0.0.9

# ── AI (async HTTP client for xAI Grok API) ──────────────────────────────────
httpx>=0.27.0

# ── Database (Postgres for production, SQLite fallback for dev) ───────────────
psycopg[binary]>=3.1.0
psycopg_pool>=3.1.0

# ── Caching + rate limiting (Redis via Upstash) ──────────────────────────────
redis[hiredis]>=5.0.0

# ── Background task queue ────────────────────────────────────────────────────
arq>=0.26.0

# ── Notifications (background scheduler for email reminders) ───────────────────
apscheduler>=3.10.0

# ── Config ─────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Natural-language date parsing (core/tools/normalize.py) ───────────────────
dateparser>=1.2.0

# ── Error Tracking ────────────────────────────────────────────────────────────
sentry-sdk[fastapi]>=2.0.0

# ── Billing (Stripe) ──────────────────────────────────────────────────────────
stripe>=8.0.0

# ── Google Calendar (optional) ────────────────────────────────────────────────
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.100.0

# ── Calendar ICS import ────────────────────────────────────────────────────────
icalendar>=5.0.0
