# ── Core AI (direct xAI Grok API — no LangChain/CrewAI needed) ────────────────
# Grok is called via direct HTTP requests (requests library below).
# Optional: keep langchain-xai if you use any LangChain tooling elsewhere.
# langchain>=0.3.0
# langchain-xai>=0.2.0

# ── Finance & Banking ──────────────────────────────────────────────────────────
plaid-python>=20.0.0
yfinance>=0.2.40
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.11.0

# ── Visualization & UI ─────────────────────────────────────────────────────────
plotly>=5.18.0
streamlit>=1.35.0
matplotlib>=3.7.0

# ── Storage & Config ───────────────────────────────────────────────────────────
python-dotenv>=1.0.0
cryptography>=41.0.0

# ── Calendar ───────────────────────────────────────────────────────────────────
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.1.0
icalendar>=5.0.0

# ── Reporting ──────────────────────────────────────────────────────────────────
fpdf2>=2.7.0
reportlab>=4.0.0

# ── HTTP ───────────────────────────────────────────────────────────────────────
requests>=2.31.0
